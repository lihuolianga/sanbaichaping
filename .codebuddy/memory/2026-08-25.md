# 2026-08-25

## git 提交推送（案件8发布收尾）
- 提交哈希 `f12da4d`，41 files changed, 3112 insertions(+), 104 deletions(-)。含案件8 md（3章改名已识别为 git rename：91/92/95）、案件8 临时脚本、chapters txt、发布工具增强脚本（publish_schedule.js 加了"仅基础检测"重试）、案件9 准备文件。
- 推送：`git -c http.proxy= -c https.proxy= push` → `14b17ba..f12da4d main -> main` 成功。
- 修正记忆：原记忆写的 `git push --config ...` 是错的，git 临时配置覆盖选项是 `-c`（放在子命令前）。已更新长期记忆 ID 65443580。
- 教训：PowerShell 命令行 `git commit -m "中文"` 会报"字符串缺少终止符"，应把提交信息写入文件后用 `git commit -F 文件`。
