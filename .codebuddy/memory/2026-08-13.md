# 2026-08-13

## 案卷目录与总进度盘点
- 盘点全书进度并生成 `案卷目录.md`（优化版案卷目录，含案号/卷目/章节/章数/闭合差评/状态）。
- 核对结果：92案章数合计 1187、差评合计 300，章号连续无断档，与总控大纲一致。
- 当前进度：第1068章已创作（案件1-83完结，闭合292条差评），完成度 90.0%。
- 待写：案件84-92（第1069-1187章，119章，8条差评），全部落在第十一、十二卷。
- 发现的问题（见 `案卷目录.md` 第五节）：
  1. 第三卷"第292章"存在两个重复文件。
  2. 第351-1068章正文文件全部集中在"第四卷"文件夹，第五至十一卷未建独立文件夹。
  3. 差评闭合前重后轻（第十二卷仅2条），属有意安排。
  4. `案件提示词/`（案件30-92完整提示词）与 `优化提示词/`（案件1-83最小修复提示词）命名体系不一致。

## 目录按卷拆分（2026-08-13）
- 用户确认后，把原堆在"第四卷"文件夹的第449-1068章正文，按标准卷划分拆到独立文件夹。
- 拆分结果（文件数=章号范围，全部对齐、零未匹配、零冲突）：
  - 第四卷：98（351-448）｜第五卷：100（449-548）｜第六卷：100（549-648）
  - 第七卷：98（649-746）｜第八卷：98（747-844）｜第九卷：98（845-942）
  - 第十卷：88（943-1030）｜第十一卷：38（1031-1068，进行中）
- 第十二卷（1110-1187）未建，尚未动笔。
- 已删第三卷重复的"第292章"草稿（无后缀 `第292章_照妖镜照出全宗是妖.md`），保留正式版 `第292章_照妖镜照出全宗是妖：差评上门.md`。判断依据：草稿只写正阳宗内部案发（余小壶、蛇鳞弟子），无七玄门掌门印/差评受理主线，与第300章"七玄门七枚掌门印重新登记激活旧校正术"的核心谜题对不上；正式版含该主线。

## 正文文件名加案件前缀（2026-08-13）
- 用户要求每章文件名前加案件信息，格式 `案件{案号}-第X章_{标题}.md`（案号不加零）。
- 已按92案章节范围映射，重命名全部1068章，0错误、0跳过。
- 遗留衔接瑕疵：删草稿后，余小壶（正式版292章未登场）在293章开头首次出现，属断档，待补出场过渡。

## 目录改为三层案件结构（2026-08-13）
- 用户最终改为三层目录：`{卷}/案件{中文数字} {案件名}/第{全局章号}章 {章名}.md`。
- 例：`第四卷/案件三十 镇国龙脉每年续费/第352章 每年续费碑写进责任范围.md`。
- 案件文件夹用中文数字序号+案件名；章节文件名保留全局阿拉伯数字章号（与正文标题一致）。
- 已重组全部1068章，0错误。第一至十一卷各建案件文件夹；第十二卷（案件87-92，1110-1187）未建。
- 首次实现把章节误重编号为案件内"第N章"，用户指出后改回全局章号。坑：正文个别标题含英文直引号（Win非法文件名字符）、部分文件带BOM，重命名须从文件名提取标题并用 utf-8-sig。

## git 提交推送（2026-08-13）
- 用户要求"git 提交推送"。提交目录重构结果。
- 提交 `5870348`：「重构目录为 卷/案件/章节 三层结构，章号改回全局编号」。
- git 将旧扁平文件→新三层文件识别为大量 100% 相似度 rename，历史清晰；1069删+1068加+1 memory改，正文零丢失。
- 推送 `git push -u origin main` 成功（`ef88a54..5870348`），已设上游跟踪。
- 注意：`git push --config http.proxy=` 写法已失效（新版 git 不支持），remote.origin.proxy 此前已设为空，直接 push 即可。
- PowerShell 中文路径会被编码成乱码导致 cd/Set-Location 失败，但 git 命令本身在工作目录下能正常跑；commit message 中文用变量 `$msg=` 传入不乱码。

## 案件84完成（第1069-1080章，闭合3条差评）
- 已完成案件84「廉价逆天改命套餐」全部12章（第1069-1080章），闭合3条差评，正文接续到第1080章。
- 目录：`第十一卷/案件八十四 廉价逆天改命套餐/`。
- 主线落点：韩九章承认"只信依赖、不信自治"；李无咎不以消灭商业为答案，而是立可监督制度（短期价/长期价、赔付储备、独立验证席三把锁、审计复议退出、长期追踪）。
- 章末钩子：因果簿翻到"拒绝轮回的鬼城 + 拒绝统一的妖族"，指向案件85《鬼城拒绝轮回，妖族拒绝统一》。
- 伏笔（未闭合，留师徒对账卷）：师父归鹤真人曾蹲阵眼刻"中止接口"未做完、且逐一劝过客户别改命。
- 质量评估后已修：全文破折号"——"64处清零（用 `.codebuddy/temp/python/fix_dash_84.py` 按语境替换为冒号/逗号/省略号）；补闭合第1076章"随机签优先砸排队者"钩子（第1079章新增"第五件，管住那口阵眼"）。
- 经验：写作铁律"严禁破折号"易被忽略，写完后必须全案搜索自检；破折号按语境分三类替换——话没说完/被打断→省略号，引出解释/列举/引述→冒号，插入语/转折→逗号。
- 下一案：案件85「鬼城拒绝轮回，妖族拒绝统一」（第1081-1094章），未动笔。

## 案件84 三章节奏优化（2026-08-13）
- 用11项网文评分标准（前7项83分+一票警戒项）逐章评估，全案平均84分（B+），短板集中在爽点情绪价值(11/15)和节奏(8/10)；低分章为1071(75)、1075(78)、1077(79)，共性：对话/探查密度高、事件推进弱、缺对抗。
- 已按"不改主剧情、只增强冲突/节奏"优化三章：
  1. 第1071章：开锁处加韩九章拦锁，李无咎拍因果簿硬开（"三百条差评加一条人命，这章程够不够"），补"目标→阻碍→对抗"。
  2. 第1075章：精简客户自陈对话，压缩回合（妇人/男人各一句带过），加快到达温楼主认罪反转的节奏。
  3. 第1077章：三方发言后插入外部危机——随机签"认得这队人、正一个一个掂量"，一个排队后生栽倒，呼应第1076章钩子并打破纯讨论。
- 优化后破折号仍0处，主剧情/证据/人物/闭合差评数未变。

## 番茄小说草稿同步（第41章，2026-08-13）
- 用户要求把第41章同步到番茄小说作者后台的"新建草稿"页（URL: `/main/writer/7665606959365639230/publish/7673438863087387160?enter_from=newdraft`）。同步成功并存入草稿箱。
- 工具栈：funnaz/fanqie-publisher-cli（基于 Playwright + 本机系统 Edge，launchPersistentContext）。
- 工具位置：`C:\Users\Administrator\fanqie-tool\`（git clone + npm install 已完成）。登录态持久化在同目录的 `.fanqie-browser-profile/`。
- 登录：首次需要用户扫码（脚本打开 Edge → 跳转登录页 → 用户扫码 → 检测已登录关闭浏览器）。后续发布复用 profile 登录态即可。
- 章节准备：md → txt（去掉 ** 加粗标记，UTF-8），文件名 `041-第41章 客户没有说完的话.txt`，首行标题、正文去掉第一行标题行。
- 关键坑（务必记住）：
  1. IDE 内置浏览器（preview_url）只能预览，不能自动操控 DOM，必须用 Playwright。
  2. 后台运行 Node（`subprocess.DETACHED_PROCESS` 或 PowerShell `Start-Process -WindowStyle Hidden`）会让 Edge GUI 卡死在初始化，看起来"卡住"；前台 `node xxx.js` 跑最可靠。
  3. 主 CLI `fanqie-publisher.js` 的 readline "按回车继续"在后台 stdin 无法配合，**直接用精简脚本**（publish_ch41.js 风格：launch→goto→waitForSelector→dumpInputs→填表→截图→保存→验证）更稳，前台约 30-60 秒跑完。
  4. PowerShell 处理中文路径有编码乱码（cd/Get-ChildItem 失败），Node 内部 fs 读中文路径无碍；建议把所有工具临时文件放纯英文路径。
- 精简脚本核心步骤（publish_ch41.js 已验证）：
  - `launchPersistentContext(USER_DATA_DIR, {headless:false, executablePath: 'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe'})`
  - `goto(url, waitUntil:'domcontentloaded')` + `waitForSelector('.ProseMirror[contenteditable=\"true\"], input[placeholder*=\"标题\"]')` + 等 3 秒
  - 标题选择器：`input.serial-input.serial-editor-input-hint-area.byte-input.byte-input-size-default`；章节号：`input.serial-input.byte-input.byte-input-size-default`；正文：`.ProseMirror[contenteditable='true']`（focus + Ctrl+A + Backspace + `keyboard.insertText`）
  - 保存按钮：文本匹配"存草稿"/"保存草稿"/"暂存"（getByRole('button', {name:/存草稿/}) 或 page.locator('text=存草稿')）
  - 验证保存：等页面出现"已保存"或"已保存到云端"
  - 全程截图（`shot_filled.png`, `shot_after_save.png`）便于诊断
- 结果：标题"客户没有说完的话"、章节号 41、正文 5552 字符（含标点）已存入番茄草稿箱，截图确认。
- **破折号警告（极重要）**：第41章原文含 **4 处破折号"——"**（位置：天——老 / 名——" / 开——韩 / 隙——是），违反项目铁律"严禁使用破折号"，已同步到草稿未修改。建议用户在番茄后台草稿箱中按 84 案经验分类替换（话没说完→省略号、引出→冒号、插入语→逗号）后再点"下一步"发布。
- 写作铁律"严禁破折号"在第41章也漏检，说明全书仍可能有残留破折号未清零，下次写作后必须全案搜索自检。
- 临时脚本与产物已全部清理（cleanup.py），工具目录只保留工具本体 + node_modules + .fanqie-browser-profile + .git 等。
- 后续发布其他章节：复用 `.fanqie-browser-profile` 登录态，把章节 md 转为对应 txt 放入 `C:/Users/Administrator/fanqie-tool/chapters/`，跑精简脚本（参考 publish_ch41.js 风格）即可。

## 番茄发布工具迁移到 .codebuddy/tools（2026-08-13 补充）
- 用户要求"把工具放到 .codebuddy/tools 目录下"。工具最终位置：`.codebuddy/tools/fanqie-publisher-cli/`（原在 `C:\Users\Administrator\fanqie-tool\`）。
- 迁移结果：C 盘旧副本已删；新位置保留了登录态 profile、node_modules、源码，并 headless 验证登录 Cookie 有效（`LOGIN_VALID: true`，未跳登录页），后续发布无需重新扫码。
- 迁移中发现两目录内容曾出现"诡异同步"（relocate.py 两边都有），经决定性测试（写 probe 文件）确认二者是独立物理副本、非 junction/symlink，DST 是 SRC 的稍早快照。结论：工具已在新位置，直接采用新位置即可。
- 坑：PowerShell 内联中文路径会乱码（`python -c` 和 `cd 中文路径` 都失败），所有涉及中文路径的操作一律用 write_to_file 写 .py 脚本（脚本内用中文字符串）+ python 运行，或用 Python subprocess(cwd=中文) 调 node。
